JSON
    texto
    .json

Cómo se definen ARRAYS
    let lista = new Array (5);
    lista[0] = 23;

    let lista = [];
    lista [0] = 23;
    lista [100];

JSON.stringify(alumne)
a texto plano(string)

JSON.parse(cadena de texto)
estructura json

Objetos Literales
const movil = {
    marca:"Huawei",
    modelo:"P40 Lote",
    RAM:6,
    disco:128,
    SO:"Android 12"
    prestaciones:["","",""],
    hardware:{
        placa:"",
        micro:"",
        carga:""
    }
}

("RAM" in movil) true of false

.keys .values
.entries

23/01/2025
Copiar
let xandro = {
    nombre : "gorgue",
    edad : 19,
    medidas : [ 80,18,30 ]
}
let aaron = xandro; NO

let copia = JSON.parse(JSON.stringify(xandro));
----
let cadena = JSON.parse(cadena);
...


para saber si es array u object
typeof()
array.isarray(xandro.medidas)



WEB STORAGE
sessionStorage -->
localStorage -->

localStorage.nota = 7;
localStorage['nota'] = 7;
localStorage.setItem('nota',7);

let valor =
    localStorage.nota
    localStorage['nota']
    localStorage.getItem('nota')

localStorage.length