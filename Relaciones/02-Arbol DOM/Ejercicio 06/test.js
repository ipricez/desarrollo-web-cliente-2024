/**
 * Crea cinco botones y coloca como value un color diferente a cada uno. Cada vez que
 * se pulse uno de los botones, el fondo de la página debe cambiar al color indicado.
 */

function cambiarColor(color) {
    document.bgColor=color;
}